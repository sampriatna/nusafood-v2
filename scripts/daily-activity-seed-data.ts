/**
 * CLI seed data — re-export from web lib (single source of truth).
 * Usage: pnpm seed:daily-activity
 */
export {
  DAILY_ACTIVITY_SEED_TEMPLATES,
  type DailyActivitySeedDef,
} from "../apps/web/lib/daily-activity-seed";
