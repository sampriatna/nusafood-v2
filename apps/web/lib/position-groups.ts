export {
  REPORT_POSITION_GROUPS,
  POSITION_GROUP_LABELS,
  normalizePositionGroup,
  isPositionGroup,
  resolveStaffPositionGroup,
  sanitizeStaffPosition,
  getPositionGroupLabel,
  type ReportPositionGroup,
} from "@nusafood/types";

export type PositionGroup = import("@nusafood/types").ReportPositionGroup;
